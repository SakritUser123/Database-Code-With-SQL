SELECT * FROM user_info;

CREATE TABLE user_info (
   username  VARCHAR(6)  NOT NULL,
   user_age   VARCHAR(6) NOT NULL ,
   user_adress VARCHAR(20) NOT NULL,
   user_phone  INTEGER(15) NOT NULL,



);
